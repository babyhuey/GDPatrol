[project]
name = "gdpatrol"
version = "1.0.0"
requires-python = ">=3.14"

[tool.ruff]
target-version = "py314"
line-length = 140
exclude = ["slack.py"]

[tool.ruff.lint]
select = ["E", "W", "F"]
ignore = ["E501", "E203"]

[tool.pytest.ini_options]
testpaths = ["tests"]
